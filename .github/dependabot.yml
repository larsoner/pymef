version: 2
updates:
  # Keep GitHub Actions (including pypa/cibuildwheel) up to date
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    groups:
      actions:
        patterns:
          - "*"
